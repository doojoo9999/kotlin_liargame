import {createTheme} from '@mantine/core';

export const theme = createTheme({
  /**
   * Add your theme customizations here.
   * You can customize colors, fonts, components, etc.
   * @see https://mantine.dev/theming/theme-object/
   */
});
